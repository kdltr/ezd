(compile -s -J ezd.scm)
(compile -s ezd.import.scm)
(compile -o ezd ezdprog.scm)

(define info '((version "0.1")))

(install-extension
 'ezd
 '("ezd.so" "ezd.import.so")
 info)

(install-program
  'ezd-command
  '("ezd")
  info)
